{#{% extends 'admin/base.html.twig' %}

{% block body %}
    <h1>Categoryattribute</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoryAttribute.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categoryAttribute.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('product_categoryattribute_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('product_categoryattribute_edit', { 'id': categoryAttribute.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}#}




{% extends 'admin/base.html.twig' %}

{% block body %}
    <h1>Product</h1>
    <div class="col-md-8 compose-right">
        <div class="inbox-details-default">
            <div class="inbox-details-heading">
                <li>
                    <a href="{{ path('product_categoryattribute_index') }}">Back to the list</a>
                </li>
            </div>
            <div class="inbox-details-body">
                <div class="alert alert-info">
                    You Can Show Detail Of This Category Attribute
                </div>
                <table>
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ categoryAttribute.id }}</td>
                        </tr>
                        <tr>
                            <th>Name</th>
                            <td>{{ categoryAttribute.name }}</td>
                        </tr>
                    </tbody>
                </table>

                <ul>
                    <li>
                      <a href="{{ path('product_categoryattribute_index') }}">Back to the list</a>
                    </li>
                    <li>
                        <a href="{{ path('product_categoryattribute_edit', { 'id': categoryAttribute.id }) }}">Edit</a>
                    </li>
                    <li>
                        {{ form_start(delete_form) }}
                        <input type="submit" value="Delete">
                        {{ form_end(delete_form) }}
                    </li>
                </ul>
            </div>
        </div>
    </div>
{% endblock %}
